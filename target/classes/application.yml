logging:
  level:
    org.hibernate:  debug
spring:
  datasource:
    url: jdbc:mysql://localhost/HibernateDemo
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.MySQL5Dialect
my:
   servers:
       - dev.bar.com
       - foo.bar.com